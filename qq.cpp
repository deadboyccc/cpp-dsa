#include <iostream>
#include <deque>
#include <list>
#include <iterator>
#include <stack>

int main(int argc, char const *argv[])j
{
    std::stack<int> firstBucket;
    std::stack<int> secondBucket;
    


    // std::deque<int> test;
    // test.push_back(10);
    // test.push_front(100);
    // test.push_back(10);
    // test.push_front(100);
    // for (std::deque<int>::iterator it = test.begin(); it != test.end(); it++)
    // {
    //     std::cout << *it << " ";
    // }
    return 0;
}
